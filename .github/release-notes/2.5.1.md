Release 2.5.1

- Preserve WODBY_API_ENDPOINT and --api-endpoint as compatibility overrides for the REST API base URL.
- Keep CI initialization on REST API calls.
- Reuse app build config when it is already returned.
- Remove unused module dependency entries.
