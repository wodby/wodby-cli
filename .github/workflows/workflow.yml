name: Build

on:
  push:
    branches:
    - master
    tags:
    - '*'
  pull_request:

permissions:
  contents: read

env:
  VERSION: ${{ github.ref_type == 'tag' && github.ref_name || 'dev' }}
  AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
  SOURCE_DIR: dist

jobs:
  build-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ linux,darwin,windows ]
        arch: [ amd64,arm64 ]
        exclude:
        - os: windows
          arch: arm64
    env:
      GOOS: ${{ matrix.os }}
      GOARCH: ${{ matrix.arch }}
    steps:
    - uses: actions/checkout@v6
    - name: Install Go
      uses: actions/setup-go@v6
      with:
        go-version-file: go.mod
    - name: build
      run: make VERSION="${VERSION}"
    - name: unit tests
      if: ${{ matrix.os == 'linux' && matrix.arch == 'amd64' }}
      run: go test ./...
    - name: test
      if: ${{ matrix.os == 'linux' && matrix.arch == 'amd64' }}
      run: make test VERSION="${VERSION}"
    - name: test native and Docker-in-Docker ownership and cache integration
      if: ${{ matrix.os == 'linux' && matrix.arch == 'amd64' }}
      run: |
        set -euo pipefail

        WODBY_DOCKER_INTEGRATION=1 go test ./cmd/wodby/ci/initialize ./pkg/cicache ./pkg/cidata ./pkg/docker

        test_home="${RUNNER_TEMP}/wodby-home"
        context="${RUNNER_TEMP}/wodby-context"
        dind_context="${RUNNER_TEMP}/wodby-dind-context"
        config="${RUNNER_TEMP}/.wodby-ci.json"
        mkdir -p "${test_home}" "${context}" "${dind_context}"

        jq -n --arg context "${context}" '{context: $context}' > "${config}"

        native_image="wodby/drupal-php:8.3-4.82.4"
        runner_uid="$(id -u)"

        git -C "${context}" init -q
        git -C "${context}" config user.name "Wodby CI"
        git -C "${context}" config user.email "ci@wodby.com"
        printf tracked > "${context}/tracked.txt"
        git -C "${context}" add tracked.txt
        git -C "${context}" -c commit.gpgsign=false commit -qm initial
        # shellcheck disable=SC2016 # Variables expand inside the container.
        HOME="${test_home}" bin/wodby \
          --ci-config-path "${config}" \
          ci run \
          --image "${native_image}" \
          --env "EXPECTED_UID=${runner_uid}" \
          -- sh -eu -c '
            test "$(id -u)" = "${EXPECTED_UID}"
            git status --short
            test "${COMPOSER_CACHE_DIR}" = /tmp/wodby-cache/composer
            printf cache-ok > /tmp/wodby-cache/composer/wodby-cache-test
          '

        test -f "${test_home}/.composer/cache/wodby-cache-test"
        test "$(stat -c %u "${test_home}/.composer/cache/wodby-cache-test")" = "${runner_uid}"

        data_container="wodby-ci-cache-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
        trap 'docker rm -fv "${data_container}" >/dev/null 2>&1 || true' EXIT
        docker create \
          --name "${data_container}" \
          --volume=/workspace \
          --volume=/tmp/wodby-cache \
          alpine /bin/true

        jq -n \
          --arg context "${dind_context}" \
          --arg data "${data_container}" \
          '{Context: $context, DataContainer: $data, WorkingDir: "/workspace"}' > "${config}"

        HOME="${test_home}" bin/wodby \
          --ci-config-path "${config}" \
          ci run \
          --image wodby/node:24 \
          -- sh -eu -c '
            test "$(id -un)" = node
            test "$(npm config get cache)" = /tmp/wodby-cache/npm
            printf dind-cache-ok > /tmp/wodby-cache/npm/wodby-dind-cache-test
          '

        test -f "${dind_context}/.wodby-ci-cache/npm/wodby-dind-cache-test"
    - name: package
      run: make package
    - name: Upload production artifacts
      uses: actions/upload-artifact@v7
      with:
        name: dist-${{ matrix.os }}-${{ matrix.arch }}
        path: dist
        retention-days: 1

  release-aws-dev:
    runs-on: ubuntu-latest
    needs: [build-test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    strategy:
      matrix:
        os: [ linux,darwin,windows ]
        arch: [ amd64,arm64 ]
        exclude:
        - os: windows
          arch: arm64
    steps:
    - name: Download artifact
      uses: actions/download-artifact@v8
      with:
        name: dist-${{ matrix.os }}-${{ matrix.arch }}
        path: dist
    - name: Release AWS dev
      uses: jakejarvis/s3-sync-action@v0.5.1
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        DEST_DIR: wodby-cli/dev/${{ matrix.os }}/${{ matrix.arch }}

  release-aws-tagged:
    runs-on: ubuntu-latest
    needs: [build-test]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    strategy:
      matrix:
        os: [ linux,darwin,windows ]
        arch: [ amd64,arm64 ]
        exclude:
        - os: windows
          arch: arm64
    steps:
    - name: Download artifact
      uses: actions/download-artifact@v8
      with:
        name: dist-${{ matrix.os }}-${{ matrix.arch }}
        path: dist
    - name: Release AWS tagged
      uses: jakejarvis/s3-sync-action@v0.5.1
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        DEST_DIR: wodby-cli/${{ github.ref_name }}/${{ matrix.os }}/${{ matrix.arch }}

  create-gh-release:
    runs-on: ubuntu-latest
    needs: [build-test]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0
    - name: Download artifacts
      uses: actions/download-artifact@v8
      with:
        pattern: dist-*
        path: dist
        merge-multiple: true
    - name: Create GitHub release
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        shopt -s nullglob
        assets=(dist/wodby-*.tar.gz)
        if [ ${#assets[@]} -eq 0 ]; then
          echo "::error::No release assets found in dist."
          exit 1
        fi

        gh release create "${GITHUB_REF_NAME}" "${assets[@]}" \
          --verify-tag \
          --target "${GITHUB_SHA}" \
          --title "${GITHUB_REF_NAME}" \
          --notes-from-tag \
          --latest=false

  build-release-image:
    runs-on: ubuntu-latest
    needs: [build-test]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/'))
    steps:
    - uses: actions/checkout@v6
    - uses: docker/setup-qemu-action@v4
      with:
        platforms: amd64,arm64
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4
    - name: Login to DockerHub
      uses: docker/login-action@v4
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build and push dev image
      uses: docker/build-push-action@v7
      if: github.ref == 'refs/heads/master'
      with:
        context: .
        push: true
        build-args: VERSION=${{ env.VERSION }}
        platforms: linux/amd64,linux/arm64
        tags: wodby/wodby-cli:dev
    - name: Build and push tagged image
      uses: docker/build-push-action@v7
      if: startsWith(github.ref, 'refs/tags/')
      with:
        context: .
        push: true
        build-args: VERSION=${{ env.VERSION }}
        platforms: linux/amd64,linux/arm64
        tags: wodby/wodby-cli:${{ env.VERSION }},wodby/wodby-cli:latest
    - name: Verify Wodby 1 image channels
      if: startsWith(github.ref, 'refs/tags/')
      run: |
        set -euo pipefail

        tagged_image="wodby/wodby-cli:${VERSION}"
        latest_image="wodby/wodby-cli:latest"
        tagged_manifest="$(docker buildx imagetools inspect --raw "${tagged_image}" | sha256sum | cut -d ' ' -f 1)"
        latest_manifest="$(docker buildx imagetools inspect --raw "${latest_image}" | sha256sum | cut -d ' ' -f 1)"

        test "${tagged_manifest}" = "${latest_manifest}"
        test "$(docker run --rm --entrypoint wodby "${tagged_image}" version)" = "${VERSION}"
        test "$(docker run --rm --entrypoint wodby "${latest_image}" version)" = "${VERSION}"
